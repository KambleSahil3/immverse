# Tic-Tac-Toe

A two-player Tic-Tac-Toe game in the browser, served via Express.

## Run locally

```bash
npm install
npm start
```

Open http://localhost:3000

## Run with Docker

```bash
docker build -t tictactoe .
docker run -p 3000:3000 -it tictactoe
```

Open http://localhost:3000

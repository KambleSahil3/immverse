apiVersion: v1
kind: Service
metadata:
  name: tictactoe
  namespace: immverse
  labels:
    app: tictactoe
spec:
  type: NodePort
  selector:
    app: tictactoe
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30000
